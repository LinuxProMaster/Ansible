В данном файле будет храниться информация о нашем inventory file. Т.е файле, в котором хранятся наши хосты
ДАННЫЙ ФАЙЛ ОБЯЗАТЕЛЬНО ДОЛЖЕН ХРАНИТЬСЯ В ОДНОЙ ДИРЕКТОРИИ С INVENTORY FILE.

[defaults]

inventory = ./"название inventory file"
host_key_checking = false (это просто надо)